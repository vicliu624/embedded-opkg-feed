PACKAGE='libatomic-1'
VERSION='14.1.1-1'
PACKAGE_ARCH='riscv64'
MAINTAINER='TDVP Device Team <devices@example.invalid>'
DESCRIPTION='Xuantie GCC libatomic shared runtime transferred from the matched TDVP K230 target'
SUPPORTED_PLATFORMS='tdvp-k230-r1'
PACKAGE_KIND='runtime'
PACKAGE_SECTION='libraries'
PACKAGE_RELEASES='r10'
PACKAGE_DEPENDS=''
PACKAGE_BUILD_DEPENDS=''
PACKAGE_AUTO_RUNTIME_DEPENDS=1
PACKAGE_BASE_OVERLAY='identical'

# This is the narrowly permitted target-derived runtime transfer described in
# docs/UPSTREAM_SOURCES.md. It imports no source archive or foreign binary:
# the build hook accepts only the byte-identical libatomic emitted by this
# platform's pinned external toolchain in the matching Buildroot target.
SOURCE_LOCK_EXEMPT_REASON='Target-derived compiler runtime transfer: libatomic.so.1.2.0 is copied only byte-for-byte from the completed tdvp-k230-r1 Buildroot target after verifying the pinned Xuantie GCC 14.1.1 toolchain, target hash, ELF ABI, SONAME, and symlink target. It is not a converted or foreign prebuilt binary.'
RUNTIME_TOOLCHAIN_VERSION='14.1.1'
RUNTIME_TOOLCHAIN_PATH='/opt/toolchain/Xuantie-900-gcc-linux-6.6.0-glibc-x86_64-V3.0.2/'
RUNTIME_LIBRARY_FILE='libatomic.so.1.2.0'
RUNTIME_LIBRARY_SHA256='a6d134d98acd4d006afe175b98bd1f46f585caa8977a9d096d59eb4ec53394b1'
